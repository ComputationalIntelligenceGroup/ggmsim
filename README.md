### How to

0. Install packages `doParallel`, `igraph` and `foreach`
1. Install the `gmat` package from CRAN with `ìnstall.packages("gmat")`
2. Clone the repository
3. Move to the repository and launch the script `data_generation.R`, this will
take a while and generate various folders with the sampled matrices.    
4. Launch the script `time_generation.R` to perform the experiment on 
execution time. 
5. Install `ggplot2`, `Matrix` and `reshape2` to produce the plots.
6. Launch the script `plot.R` 
